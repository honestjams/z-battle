export { applyIntent, checkWinLoss } from './engine';
export { legalMoves } from './legalMoves';
export { createInitialState, makeFighterInstance } from './setup';
export { getEffectiveStats } from './buffs';
export { getCard, DECKS, CONSTANTS } from './cards';
export type { GameState, PlayerState, FighterInstance, Intent, CardDef, PlayerId, Phase } from './types';
